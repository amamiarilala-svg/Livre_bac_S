\chapter{Entrainement}

	
\section*{Sujet type bac n°1}

\subsection*{Partie A : Équations Différentielles}
\begin{enumerate}
	\item $(E_{0})$ désigne l'équation différentielle : 
	\begin{equation*}
		y'' + 2y' + y = 0
	\end{equation*}
	Déterminer les solutions générales de $(E_{0})$. \hfill (1,0 pt)
	
	\item $(E)$ est l'équation différentielle : 
	\begin{equation*}
		y'' + 2y' + y = 2e^{-x}
	\end{equation*}
	\begin{enumerate}
		\item Vérifier que la fonction $h$ définie sur $\mathbb{R}$ par $h(x) = x^2e^{-x}$ est une solution particulière de $(E)$. \hfill (0,5 pt)
		\item Démontrer que $\varphi$ est une solution de $(E)$ si et seulement si $g = \varphi - h$ est solution de $(E_{0})$. \hfill (0,75 pt)
		\item Déterminer toutes les solutions de $(E)$. \hfill (0,5 pt)
		\item Déterminer la solution $f_{0}$ de $(E)$ satisfaisant aux conditions initiales : $f_{0}(0) = 4$ et $f_{0}'(0) = 0$. \hfill (0,75 pt)
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B : Étude de fonction}
On considère la fonction $f$ définie par : 
\begin{equation*}
	f(x) = (x+2)^{2}e^{-x}
\end{equation*}
On désigne par $(\mathcal{C})$ sa courbe représentative dans le plan muni d'un repère orthonormal (unité : 1 cm).
\begin{enumerate}
	\item Étudier les variations de $f$ et tracer $(\mathcal{C})$ avec soin. \hfill (1,5 pts)
	\item En remarquant que $f$ est une solution de l'équation différentielle $(E)$, déterminer une primitive $F$ de $f$ sur $\mathbb{R}$. \hfill (1,0 pt)
	\item Pour tout entier naturel $n$, on pose $I_{n} = \int_{0}^{n} f(t)dt$.
	\begin{enumerate}
		\item Exprimer $I_{n}$ en fonction de $n$ et en donner une interprétation graphique. \hfill (0,75 pt)
		\item Étudier la convergence de la suite $(I_{n})$, puis en déduire l'aire de l'ensemble des points $M(x;y)$ tels que $x \le 0$ et $0 \le y \le f(x)$. \hfill (1,0 pt)
	\end{enumerate}
\end{enumerate}

\subsection*{Partie C : Convergence de suites}
On étudie la suite $(u_{n})$ définie pour $n \in \mathbb{N}^*$ par : 
\begin{equation*}
	u_{n} = \frac{1}{n^{3}} \sum_{k=1}^{n} (k+2n)^{2} e^{-\frac{k}{n}}
\end{equation*}
\begin{enumerate}
	\item Vérifier que pour tout $n \in \mathbb{N}^*$, on a : 
	\begin{equation*}
		u_{n} = \frac{1}{n} \sum_{k=1}^{n} f\left(\frac{k}{n}\right) \quad \text{et} \quad \frac{1}{n} \sum_{k=0}^{n-1} f\left(\frac{k}{n}\right) = u_{n} + \frac{4e-9}{ne}
	\end{equation*} \hfill (1,0 pt)
	\item Établir que, pour tout $n \in \mathbb{N}^*$ et tout $k \in \{0, \dots, n-1\}$, on a : 
	\begin{equation*}
		\frac{1}{n}f\left(\frac{k+1}{n}\right) \le \int_{\frac{k}{n}}^{\frac{k+1}{n}} f(t)dt \le \frac{1}{n}f\left(\frac{k}{n}\right)
	\end{equation*} \hfill (1,0 pt)
	\item Démontrer que pour tout $n \in \mathbb{N}^*$ : 
	\begin{equation*}
		u_{n} \le \int_{0}^{1} f(t)dt \le u_{n} + \frac{4e-9}{ne}
	\end{equation*} \hfill (1,0 pt)
	\item En déduire que : 
	\begin{equation*}
		I_{1} - \frac{4e-9}{ne} \le u_{n} \le I_{1}
	\end{equation*} \hfill (0,5 pt)
	\item Étudier la convergence de la suite $(u_{n})$ et préciser sa limite. \hfill (0,5 pt)
\end{enumerate}




\section*{Sujet type bac n°2}

\subsection*{Partie A : Analyse}
Soit $f$ la fonction de la variable réelle $x$ définie sur l'intervalle $[0;+\infty[$ par $f(x) = \frac{1}{2\sqrt{x^2+1}}$ et $h$ la fonction définie sur le même intervalle par $h(x) = \ln(\sqrt{x+\sqrt{x^2+1}})$.
\begin{enumerate}
	\item Démontrer que $f$ est une bijection de $[0;+\infty[$ vers $]0; 1/2]$. \hfill (1,0 pt)
	\item Le plan est rapporté à un repère orthonormal $(O, \vec{i}, \vec{j})$, l'unité graphique étant égale à 2 cm. Tracer dans ce repère les courbes représentatives de $f$ et de $f^{-1}$. \hfill (1,5 pts)
	\item \begin{enumerate}
		\item Calculer la dérivée de $h$. \hfill (0,5 pt)
		\item En déduire l'aire de la partie du plan limitée par l'axe des abscisses, l'axe des ordonnées, la droite d'équation $x = 1$ et la courbe représentative de $f$. \hfill (1,0 pt)
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B : Géométrie et Isométries}
Le plan est orienté. $ABC$ est un triangle équilatéral tel que : $AB = BC = CA = 1$ et $\text{Mes}(\overrightarrow{AB}, \overrightarrow{AC}) = \frac{\pi}{3}$. $A'$ désigne le milieu du segment $[BC]$ et $G$ l'isobarycentre des points $A, B$ et $C$.
\begin{enumerate}
	\item Construire $G$ et calculer $GA'$. \hfill (1,0 pt)
	\item \begin{enumerate}
		\item Déterminer la nature et les éléments caractéristiques de l'ensemble $(E)$ des points $M$ du plan tels que : $MA^2 + MB^2 + MC^2 = 1,25$. \hfill (1,5 pts)
		\item Tracer $(E)$ sur la figure précédente.
	\end{enumerate}
	\textit{Dans toute la suite, $r$ désigne la rotation de centre $G$ qui transforme $C$ en $A$ et $h$ l'application qui, à tout point $M$ du plan, associe le point $M'$ tel que : $\overrightarrow{MM'} = \overrightarrow{MA} + \overrightarrow{MB} + \overrightarrow{MC}$. On pose : $s = h \circ r$.}
	\item \begin{enumerate}
		\item Démontrer que $h$ est une homothétie dont on précisera le centre et le rapport. \hfill (1,0 pt)
		\item Déterminer puis tracer sur la même figure que précédemment l'image de $(E)$ par $h$. \hfill (0,5 pt)
		\item Préciser la nature et les éléments caractéristiques de $s$. \hfill (1,0 pt)
	\end{enumerate}
	\item On note $I$ le symétrique de $A'$ par rapport à $C$, $J$ le point de la demi-droite $[A'A)$ tel que : $A'J = 1$, $\vec{u} = \overrightarrow{A'I}$ et $\vec{v} = \overrightarrow{A'J}$.
	\begin{enumerate}
		\item Démontrer que $(A', \vec{u}, \vec{v})$ est un repère orthonormal. \hfill (0,75 pt)
		\item $M$ étant un point d'affixe $z$ et $M'$ son image par $s$, exprimer l'affixe $z'$ de $M'$ en fonction de celle de $M$. En déduire l'expression analytique de $s$ dans le repère $(A', \vec{u}, \vec{v})$. \hfill (1,25 pts)
	\end{enumerate}
\end{enumerate}






\section*{Sujet type bac n°3}

\subsection*{Partie A : Étude de fonction}
On considère la fonction numérique $f$ de la variable réelle $x$ définie par $$f(x) = \frac{e^x}{x+2}$$. On désigne par $(\mathcal{C})$ la courbe représentative de $f$ dans un repère orthonormal $(O, I, J)$, l'unité graphique étant égale à 2 cm.
\begin{enumerate}
	\item \begin{enumerate}
		\item Déterminer l'ensemble de définition $D$ de $f$. Étudier les limites de $f$ aux bornes de $D$. Préciser les asymptotes de la courbe $(\mathcal{C})$. \hfill (1,0 pt)
		\item Étudier les variations de $f$ ; dresser son tableau de variation. Déterminer une équation de la tangente $(T)$ à $(\mathcal{C})$ au point d'abscisse 0. \hfill (1,5 pts)
		\item Construire avec soin $(T)$ et la courbe $(\mathcal{C})$. \hfill (1,0 pt)
	\end{enumerate}
	\item On se propose de montrer que l'équation $f'(x) = x$ admet sur $[0; 1]$ une solution unique.
	\begin{enumerate}
		\item Étudier les variations de la fonction dérivée $f'$ sur $[0; 1]$. Démontrer que pour tout $x$ de l'intervalle $[0; 1]$, on a : $\frac{1}{4} \le f'(x) \le \frac{2}{3}$. \hfill (1,0 pt)
		\item Étudier les variations de la fonction numérique $g$ définie sur $[0; 1]$ par $g(x) = f(x) - x$. Démontrer que l'équation $f(x) = x$ admet dans $[0; 1]$ une solution unique $\alpha$. Vérifier que $\frac{1}{2} \le \alpha \le \frac{e}{3}$. \hfill (1,5 pts)
	\end{enumerate}
	\item On se propose de déterminer une valeur approchée de $\alpha$. Soit $(u_n)$ la suite définie par : $u_0 = \frac{1}{2}$ et pour tout $n \in \mathbb{N}, u_{n+1} = f(u_n)$.
	\begin{enumerate}
		\item Démontrer par récurrence que pour tout $n \in \mathbb{N}, \frac{1}{2} \le u_n \le \frac{e}{3}$. \hfill (0,5 pt)
		\item En utilisant l'inégalité des accroissements finis, démontrer que pour tout $n \in \mathbb{N}, |u_{n+1} - \alpha| \le \frac{2}{3} |u_n - \alpha|$. En déduire que pour tout $n \in \mathbb{N}, |u_n - \alpha| \le (\frac{2}{3})^n |u_0 - \alpha| \le (\frac{2}{3})^{n+1}$. \hfill (1,0 pt)
		\item Démontrer que la suite $(u_n)$ est convergente. Quelle est sa limite ? \hfill (0,5 pt)
		\item Déterminer un entier $n_0$ tel que : si $n \ge n_0$, alors $|u_n - \alpha| \le 10^{-2}$. \hfill (0,5 pt)
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B : Encadrement d'intégrale}
Ne connaissant pas de primitive de la fonction $f$ sur $[0; 1/2]$, on se propose de déterminer un encadrement de l'intégrale $I = \int_0^{1/2} f(x) dx$.
\begin{enumerate}
	\item \begin{enumerate}
		\item Justifier l'existence de $I$ et en donner une interprétation graphique. \hfill (0,5 pt)
		\item On pose $J = \int_0^{1/2} (2-x)e^x dx$ et $K = \int_0^{1/2} x^2 f(x) dx$. \\
		Vérifier que $4I = J + K$. \hfill (0,75 pt)
		\item Calculer $J$. \hfill (0,75 pt)
		\item Quelle est l'image par $f$ du segment $[0; 1/2]$ ? En déduire un encadrement de $K$ par deux intégrales simples que l'on calculera.\hfill(1,0 pt)
		\item À l'aide des questions précédentes, écrire un encadrement de l'intégrale $I$. En déduire un encadrement de $I$, d'amplitude $10^{-2}$ par des nombres décimaux. \hfill (1,0 pt)
	\end{enumerate}
\end{enumerate}




\section*{Sujet type bac n°4}

\subsection*{Partie A}

On considère, dans le plan complexe $\mathscr{P}$, les points $A$ d'affixe $z_A = 1$,
$M$ d'affixe $z$ et $N$ d'affixe $z_N = \ii z - (1+\ii)$.
On note $T_\lambda$ l'application qui, à tout point $M$ d'affixe $z$, associe le point $M'$
barycentre des points pondérés $(M,\lambda)$, $(N,-\lambda)$ et $(A,1)$, où $\lambda$ est un nombre réel non nul.

\begin{enumerate}
	\item Démontrer que, pour tout point $M$ du plan, le point $N$ est l'image de $M$ par
	une rotation dont on précisera le centre et l'angle.

	\item
	\begin{enumerate}
		\item Démontrer que l'affixe $z'$ de $M'$ est telle que :
		\[
		z' = \lambda(1-\ii)z + \lambda(1+\ii) + 1.
		\]
		\item Démontrer que $T_\lambda$ est une similitude directe dont on précisera l'affixe
		$\omega$ du centre $\Omega$, le rapport et l'angle.
		Pour quelles valeurs de $\lambda$, $T_\lambda$ est-elle une rotation ?
		Donner, dans chaque cas, son angle et l'affixe de son centre.
		\item Exprimer les coordonnées $(x'\,;y')$ de $M'$ en fonction des coordonnées $(x\,;y)$ de $M$.
	\end{enumerate}

	\item Le nombre réel $\lambda$ étant strictement positif, on lui associe le point $P$
	de coordonnées $(-\ln\lambda\,;\ln\lambda)$.
	Soit $P'$ le point tel que : $P' = T_\lambda(P)$.
	\begin{enumerate}
		\item Déterminer les coordonnées de $P'$ en fonction de $\lambda$.
		\item Démontrer que, lorsque $\lambda$ décrit $\R_+^*$, l'ensemble des points $P'$
		est la courbe $(\mathscr{C})$ d'équation :
		\[
		y = 2(x-1)\ln(x-1) + (x-1).
		\]
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B}

Soit $f$ la fonction définie sur $\R_+$ par :
\[
\begin{cases}
	f(x) = 2x\ln x + x, & \text{si } x > 0 \\
	f(0) = 0
\end{cases}
\]
On note $(\Gamma)$ sa courbe représentative dans un repère orthonormal $(O,\vec{\imath},\vec{\jmath})$,
l'unité graphique étant égale à $10$ cm.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item La fonction $f$ est-elle continue à droite en $0$ ?
		Déterminer la limite à droite en $0$ de $\dfrac{f(x)}{x}$.
		Donner une interprétation géométrique du résultat.
		$f$ est-elle dérivable en $0$ ?
		\item Étudier les variations de la fonction $f$ et préciser le nombre réel $x_0 \neq 0$
		tel que $f(x_0) = 0$.
		\item Tracer $(\Gamma)$. Déterminer une transformation du plan par laquelle la courbe
		$(\mathscr{C})$ se déduit de la courbe $(\Gamma)$.
	\end{enumerate}

	\item Soit $a \in \left]0\,;\e^{-1/2}\right[$.
	\begin{enumerate}
		\item Calculer : $\displaystyle I(a) = \int_a^{\e^{-1/2}} (2x\ln x + x)\,dx$.
		\item Déterminer $\displaystyle\lim_{a\to0} |I(a)|$ et en donner une interprétation géométrique.
		\item Soit $D$ le domaine formé des points $M(x\,;y)$ du plan tels que :
		\[
		\begin{cases}
			a \leqslant x \leqslant \e^{-1/2} \\
			0 \leqslant y \leqslant f(x)
		\end{cases}
		\]
		On fait tourner $D$ autour de l'axe des abscisses. Calculer le volume $V(a)$ ainsi obtenu.

		Déterminer $\displaystyle\lim_{a\to0} V(a)$ et en donner une valeur approximative.
		(On donne : $\e^{-1/2} \approx 0,74$ et $\e^{-3/2} \approx 0,2$.)
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°5}

\subsection*{Partie A}

On considère la fonction numérique réelle $f$ définie par $f(x) = x - \ln|x|$, où $\ln$ désigne le logarithme népérien et $|x|$ la valeur absolue du nombre réel $x$. On note $(\mathscr{C})$ la courbe représentative de $f$ dans le plan muni d'un repère orthonormé $(O,\vec{\imath},\vec{\jmath})$ dont l'unité vaut $2$ cm.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Calculer les limites de $f$ aux bornes de son ensemble de définition.
		\item Étudier le sens de variation de $f$. (On dressera son tableau de variation.)
	\end{enumerate}

	\item
	\begin{enumerate}
		\item Démontrer que $(\mathscr{C})$ coupe l'axe des abscisses en un seul point $A$ dont on notera l'abscisse $\alpha$ et vérifier que : $\alpha \in \left]-1\,;-\dfrac{1}{2}\right[$.
		\item Étudier la position relative de la courbe $(\mathscr{C})$ et de la droite $(D)$ d'équation $y = x$.
		\item Tracer la courbe $(\mathscr{C})$, la droite $(D)$, ainsi que la tangente à $(\mathscr{C})$ au point d'abscisse $-1$.
	\end{enumerate}

	\item
	\begin{enumerate}
		\item Pour tout nombre réel $k$ élément de $]0\,;1[$, calculer : $\displaystyle A(k) = 4\int_k^1 f(x)\,dx$.
		\item Déterminer $\displaystyle\lim_{\substack{k\to0\\k>0}} A(k)$. Interpréter graphiquement ce résultat.
	\end{enumerate}

	\item $a$ étant un nombre réel non nul quelconque, on note $M$ et $M'$ les points de $(\mathscr{C})$ d'abscisses respectives $a$ et $-a$, $(T)$ et $(T')$ les tangentes respectives à $(\mathscr{C})$ en $M$ et $M'$.
	\begin{enumerate}
		\item Exprimer à l'aide de $a$ les coordonnées de $N$, point d'intersection de $(T)$ et $(T')$.
		\item Quel ensemble décrit $N$ lorsque $a$ parcourt $\R^*$ ?
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B}

On note $\C^*$ l'ensemble des nombres complexes non nuls et on considère l'application :
\[
F : \C^* \to \C, \qquad z \mapsto -\ln|z|.
\]

\begin{enumerate}
	\item Dans chacun des cas suivants, déterminer l'ensemble des nombres complexes vérifiant l'équation :
	\begin{enumerate}
		\item $F(z) = 0$
		\item $F(z) = z$
		\item $F(z) = \bar{z}$, où $\bar{z}$ est le conjugué de $z$.
	\end{enumerate}

	\item Soit $G$ l'application du plan dans lui-même qui à tout point $M$ d'affixe $z$ associe le point $M'$ d'affixe $z' = F(z)$. On note $\mathscr{C}_R$ le cercle de centre $O$ et de rayon $R$.
	\begin{enumerate}
		\item Pour tout point de $\mathscr{C}_R$ d'affixe $z$, donner une écriture simplifiée de $F(z)$.
		\item En déduire $G(\mathscr{C}_R)$. Pour quelle(s) valeur(s) de $R$ a-t-on $G(\mathscr{C}_R) = \mathscr{C}_R$ ?
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°6}

Le plan est rapporté à un repère orthonormé $(O,\vec{\imath},\vec{\jmath})$, l'unité graphique étant égale à $2$ cm.

\subsection*{Partie A}

\begin{enumerate}
	\item Soit $f$ la fonction numérique de la variable réelle définie par $f(x) = x - 1 + \ln(3-x)$ et $(\mathscr{C})$ sa courbe représentative.
	\begin{enumerate}
		\item Déterminer l'ensemble de définition $D_f$ de la fonction $f$.
		\item Étudier les variations de $f$ en précisant les limites de $f$ aux bornes de $D_f$ et établir son tableau de variation.
		\item Soit $(D)$ la droite d'équation $y = x-1$. Étudier la position de $(\mathscr{C})$ par rapport à $(D)$ ; déterminer les coordonnées de leur point d'intersection.
		\item Tracer la droite $(D)$ et la courbe $(\mathscr{C})$.
	\end{enumerate}
	\item Soit $E$ le domaine plan délimité par la courbe $(\mathscr{C})$, la droite d'équation $x = 0$ et la droite $(D)$. Calculer l'aire $\mathscr{A}$ de $E$.
\end{enumerate}

\subsection*{Partie B}

Dans cette partie, à tout point $M$ du plan de coordonnées $(x\,;y)$, on associe son affixe $z = x+\ii y$.
Soit $S$ l'application du plan dans lui-même qui à tout point $M$ d'affixe $z$ associe le point $M_1$ d'affixe $z_1$ telle que : $z_1 = (-1+\ii)z + 1 + 4\ii$.

\begin{enumerate}
	\item Donner la nature de $S$ et déterminer ses éléments caractéristiques.
	\item Calculer les coordonnées $x$ et $y$ du point $M$ en fonction des coordonnées $x_1$ et $y_1$ du point $M_1$.
	\item Déterminer les équations des transformées par $S$ de la droite d'équation $x = 0$ et de la droite $(D)$.
	\item Soit $g$ la fonction numérique de la variable réelle définie par $g(x) = -x-1+2\e^x$ et $(\Gamma)$ sa courbe représentative dans le repère $(O,\vec{\imath},\vec{\jmath})$.

	Soit $M$ un point de $(\mathscr{C})$ et $M_1 = S(M)$. Vérifier que l'abscisse $x_1$ de $M_1$ peut prendre toute valeur réelle et démontrer que $M_1$ est sur la courbe $(\Gamma)$. Réciproquement, si $M_1$ est sur la courbe $(\Gamma)$, montrer qu'il existe un point $M$ de la courbe $(\mathscr{C})$ tel que $M_1 = S(M)$. En déduire que $(\Gamma)$ est l'image de $(\mathscr{C})$ par $S$.
\end{enumerate}

\subsection*{Partie C}

\begin{enumerate}
	\item Établir le tableau de variation de la fonction $g$ en précisant les limites de $g$ en $-\infty$ et en $+\infty$.
	\item Démontrer que la droite d'équation $y = -x-1$ est asymptote à $(\Gamma)$.
	\item Tracer la courbe $(\Gamma)$ et la droite d'équation $y = -x+5$ dans le même repère $(O,\vec{\imath},\vec{\jmath})$, mais avec une couleur différente de celle utilisée pour $(\mathscr{C})$ et $(D)$.
	\item On appelle $E_1$ la partie du plan délimitée par la courbe $(\Gamma)$ et les droites d'équations respectives $x = 0$ et $y = -x+5$.

	On admet que la partie $E$ est transformée en $E_1$ par $S$. Exprimer l'aire de la partie $E_1$ en fonction de l'aire de $E$, puis donner sa valeur.
\end{enumerate}


\section*{Sujet type bac n°7}

\subsection*{Partie A}

Soit la fonction $f$ définie par : $f(0)=0$, $f(x) = \dfrac{x\ln x}{x+1}$, si $x>0$.

\begin{enumerate}
	\item Étudier la continuité et la dérivabilité de $f$ en $0$.
	\item Soit $\varphi$ la fonction définie sur $]0\,;+\infty[$ par : $\varphi(x) = \ln x + x + 1$.
	\begin{enumerate}
		\item Étudier les variations de $\varphi$.
		\item Démontrer que l'équation $\varphi(x)=0$ admet une solution unique $\beta$ telle que : $0,27 \leqslant \beta \leqslant 0,28$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Exprimer $f'(x)$ en fonction de $\varphi(x)$. En déduire les variations de $f$.
		\item Vérifier que $f(\beta) = -\beta$.
	\end{enumerate}
	\item Tracer la courbe représentative de $f$ dans le plan muni du repère orthonormé $(O,I,J)$. On placera les points d'abscisses $1$ ; $3$ ; $4$ ; $\e^2$ ; $12$.

	(On prendra : $\ln 0,27 \approx -1,31$ ; $\ln 0,28 \approx -1,27$ ; $\ln 2 \approx 0,7$ ; $\ln 3 \approx 1,1$ ; $\ln 5 \approx 1,6$.)
\end{enumerate}

\subsection*{Partie B}

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Démontrer que l'équation $f(x)=1$ admet une solution unique $\alpha$ dans $[3\,;4]$.
		\item Démontrer que les équations $f(x)=1$ et $\e^{1+\frac1x}=x$ sont équivalentes.
	\end{enumerate}
	\item Soit $g$ la fonction définie pour tout nombre réel strictement positif $x$ par : $g(x) = \e^{1+\frac1x}$.
	\begin{enumerate}
		\item Étudier les variations de $g$.
		\item Démontrer que pour tout $x$ élément de $[3\,;4]$, $g(x)$ est un élément de $[3\,;4]$.
		\item Démontrer que : $\forall x \in [3\,;4]$, $g'(x) \leqslant \dfrac12$.

		(On prendra : $\e^{3/4} \approx 3,8$ et $\e^{5/4} \approx 3,49$.)
	\end{enumerate}
	\item Soit $(u_n)$ la suite définie par : $u_0=3$ et la relation de récurrence $u_{n+1}=g(u_n)$.
	\begin{enumerate}
		\item Démontrer que, pour tout entier positif ou nul $n$ : $|u_{n+1}-\alpha| \leqslant \dfrac12|u_n-\alpha|$.

		En déduire que : $|u_n-\alpha| \leqslant \dfrac{1}{2^n}$.
		\item Démontrer que la suite $(u_n)$ est convergente. Calculer sa limite.
		\item Pour quelles valeurs de $n$, $u_n$ est-elle une valeur approchée de $\alpha$ à $10^{-2}$ près ?
	\end{enumerate}
\end{enumerate}

\subsection*{Partie C}

\begin{enumerate}
	\item Soit $n$ un entier naturel non nul.

	Démontrer que l'équation $f(x)=n$ admet une solution $\alpha_n$ et une seule.

	Placer $\alpha_1$ et $\alpha_2$ dans le même repère que précédemment.
	\item
	\begin{enumerate}
		\item Démontrer que, pour tout entier naturel non nul $n$, on a : $\alpha_n \geqslant \e^n$.
		\item Démontrer que, pour tout entier naturel non nul $n$, $f(\alpha_n)=n$ est équivalent à $\ln\!\left(\dfrac{\alpha_n}{\e^n}\right) = \dfrac{n}{\alpha_n}$.
		\item Déduire des questions 2.a) et 2.b) que la suite $\left(\dfrac{\alpha_n}{\e^n}\right)$ $(n\in\N^*)$ est convergente et calculer sa limite.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°8}

Dans ce problème, $n$ est un entier naturel différent de zéro et on considère la famille de fonctions $f_n$ définies sur $[0\,;+\infty[$ comme suit : $f_n(x)=x^n\ln\!\left(1+\dfrac1x\right)$, si $x\neq0$, $f_n(0)=0$.

Pour les représentations graphiques, le plan est muni d'un repère orthonormal d'unité graphique $2$ cm, et on note $(\mathscr{C}_n)$ la courbe représentative de la fonction $f_n$.

\subsection*{Partie A}

Dans cette première partie, on se propose d'étudier la fonction $f_1$.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Étudier la continuité de $f_1$ en $0$ et son comportement en $+\infty$.
		\item Étudier le comportement du rapport $\dfrac{f_1(x)}{x}$ lorsque $x$ tend vers $0$. Que peut-on conclure pour la fonction $f_1$ en $0$ et la courbe $(\mathscr{C}_1)$ ?
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Justifier la dérivabilité de $f_1$ sur $]0\,;+\infty[$. Calculer $f_1'(x)$ puis $f_1''(x)$ pour tout nombre réel appartenant à $]0\,;+\infty[$.
		\item Étudier successivement les variations de $f_1'$ et de $f_1$ et dresser le tableau de variation de $f_1$.
	\end{enumerate}
	\item Tracer la courbe $(\mathscr{C}_1)$, son asymptote et la tangente au point $O$.
\end{enumerate}

\subsection*{Partie B}

La seconde partie est consacrée à l'étude des fonctions $f_n$ lorsque $n \geqslant 2$.

\begin{enumerate}
	\item Étudier la continuité et la dérivabilité de $f_n$ en $0$ puis le comportement de $f_n$ en $+\infty$.
	\item
	\begin{enumerate}
		\item Justifier la dérivabilité de $f_n$ sur $]0\,;+\infty[$.

		Démontrer que : $f_n'(x) = x^{n-1}g_n(x)$, où $g_n$ est une fonction définie sur $]0\,;+\infty[$ que l'on déterminera.
		\item Étudier les variations de la fonction $g_n$ et en déduire le signe de $g_n(x)$.
		\item Dresser le tableau de variation de $f_n$.
	\end{enumerate}
\end{enumerate}

\subsection*{Partie C}

La troisième partie porte sur les courbes $(\mathscr{C}_n)$ lorsque $n \geqslant 2$.

\begin{enumerate}
	\item Quelle est la tangente à $(\mathscr{C}_n)$ en $O$, origine du repère ?
	\item Étudier la position relative de $(\mathscr{C}_n)$ et de $(\mathscr{C}_{n+1})$ pour $n \geqslant 2$.

	Quelle est la position de $(\mathscr{C}_1)$ par rapport à toutes les courbes $(\mathscr{C}_n)$ ?
	\item
	\begin{enumerate}
		\item Déterminer : $\displaystyle\lim_{x\to+\infty}\left[x^2\ln\!\left(1+\frac1x\right) - \left(x-\frac12\right)\right]$.

		Que peut-on en conclure pour $(\mathscr{C}_2)$ ?
		\item Préciser la position de $(\mathscr{C}_2)$ par rapport à la droite d'équation : $y = x - \dfrac12$.
	\end{enumerate}
	\item Tracer $(\mathscr{C}_2)$ et $(\mathscr{C}_3)$ dans le même repère que $(\mathscr{C}_1)$.
\end{enumerate}


\section*{Sujet type bac n°9}

\subsection*{Partie A}

On considère la fonction numérique $g$ définie sur l'intervalle $I=\left[-\dfrac12\,;+\infty\right[$ par : $g(x) = \ln(1+x) - x + \dfrac{x^2}{2} - \dfrac{x^3}{3}$.

\begin{enumerate}
	\item Démontrer que pour tout $t$ de $I$ on a : $g'(t) = \dfrac{-t^3}{1+t}$.
	\item Déduire de 1. que pour tout $t$ de $I$, on a :
	\[
	\begin{cases}
		-2t^3 \leqslant g'(t) \leqslant 0, & \text{si } t \geqslant 0 \\
		0 \leqslant g'(t) \leqslant -2t^3, & \text{si } -\dfrac12 \leqslant t \leqslant 0
	\end{cases}
	\]
	puis, en intégrant, démontrer que pour tout $x$ de $I$ on a : $-\dfrac12 x^4 \leqslant g(x) \leqslant 0$.
\end{enumerate}

\subsection*{Partie B}

Soit $f$ la fonction numérique définie sur $]-1\,;+\infty[$ par : $f(x)=\dfrac{x-\ln(1+x)}{x^2}$, si $x\neq0$, $f(0)=\dfrac12$.

On note $(\mathscr{C})$ la courbe représentative de $f$ dans le plan rapporté à un repère orthonormal $(O,\vec{\imath},\vec{\jmath})$ (unité : $2$ cm).

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Vérifier que pour tout $x \geqslant -\dfrac12$ et $x\neq0$,
		\[
		f(x) = -\frac{g(x)}{x^2} + \frac12 - \frac{x}{3}.
		\]
		\item En utilisant l'inégalité trouvée en A.2. démontrer que $f$ est dérivable en $0$ et déterminer une équation de la tangente $(T)$ à $(\mathscr{C})$ au point d'abscisse $0$.
		\item $f$ est-elle continue en $0$ ? Justifier votre réponse.
	\end{enumerate}
	\item Soit $h$ la fonction définie sur $]-1\,;+\infty[$ par : $h(x) = \dfrac{-x^2-2x}{1+x} + 2\ln(1+x)$.
	\begin{enumerate}
		\item Étudier le sens de variation de $h$. Calculer $h(0)$ et en déduire le signe de $h$ sur $]-1\,;+\infty[$.
		\item Démontrer que pour tout $x$ appartenant à $]-1\,;0[\,\cup\,]0\,;+\infty[$, on a : $f'(x)=\dfrac{h(x)}{x^3}$.
		\item Dresser le tableau de variation de $f$ en précisant les limites de $f$ aux bornes de son ensemble de définition.
	\end{enumerate}
	\item Construire la courbe $(\mathscr{C})$ et la tangente $(T)$. Préciser les asymptotes à $(\mathscr{C})$.
	\item Calculer en cm$^2$ l'aire $\mathscr{A}(\lambda)$ du domaine plan limité par les droites d'équations $x=1$, $x=\lambda$ $(\lambda>1)$, l'axe des abscisses et la courbe $(\mathscr{C})$. Déterminer $\displaystyle\lim_{\lambda\to+\infty} \mathscr{A}(\lambda)$.
\end{enumerate}

\subsection*{Partie C}

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Démontrer que la fonction $\varphi$ définie sur $]-1\,;+\infty[$, par $\varphi(x)=f(x)-x$ est continue et strictement croissante.
		\item En déduire que l'équation $f(x)=x$ admet une solution unique $\alpha$ dans $]-1\,;+\infty[$ et que $\dfrac14 \leqslant \alpha \leqslant 1$.
	\end{enumerate}
	\item Sachant que pour tout $x \geqslant 0$ on a $x-\dfrac{x^2}{2} \leqslant \ln(1+x)$, démontrer que pour tout $x \geqslant 0$ on a : $-\dfrac{1}{1+x} \leqslant f'(x) \leqslant 0$ ; puis que pour tout $x$ de l'intervalle $\left[\dfrac14\,;1\right]$, $|f'(x)| \leqslant \dfrac45$.

	(On pourra utiliser les résultats de B2).
	\item Soit $J$ l'intervalle $\left[\dfrac14\,;1\right]$. Démontrer que $f(J)\subset J$.
	\item On définit la suite $(u_n)_{n\in\N}$ par : $u_0=\dfrac12$ et pour tout $n\in\N$, $u_{n+1}=f(u_n)$.
	\begin{enumerate}
		\item Justifier que pour tout $n\in\N$, $u_n\in J$.
		\item Démontrer que pour tout $n\in\N$, $|u_{n+1}-\alpha|\leqslant\dfrac45|u_n-\alpha|$ ; puis que pour tout $n\in\N$, $|u_n-\alpha|\leqslant\dfrac34\left(\dfrac45\right)^n$.
		\item En déduire que la suite $(u_n)_{n\in\N}$ converge et déterminer sa limite.
		\item Déterminer l'entier $n_0$ tel que pour tout $n\geqslant n_0$, $u_n$ est une valeur approchée de $\alpha$ à $10^{-1}$ près.
	\end{enumerate}
\end{enumerate}

(On donne : $0,69\leqslant\ln2\leqslant0,70$ ; $0,22\leqslant\ln\dfrac54\leqslant0,23$ ; $\ln\dfrac{4}{30}\leqslant-2,01$.)


\section*{Sujet type bac n°10}

Soit $f$ la fonction numérique de la variable réelle $x$ définie par :
\[
\begin{cases}
	f(x) = 2\e^{x+1}-x^2-2x-2, & \text{si } x<-1 \\
	f(x) = 1+\sin^3(\pi x), & \text{si } -1\leqslant x\leqslant1 \\
	f(x) = x+\ln\!\left(\dfrac{1+x}{2x}\right), & \text{si } x>1
\end{cases}
\]
On désigne par $(\mathscr{C})$ la courbe représentative de $f$ dans le plan rapporté à un repère orthonormé $(O,\vec{\imath},\vec{\jmath})$, l'unité graphique étant égale à $2$ cm.

\begin{enumerate}
	\item Déterminer l'ensemble de définition $D_f$ de $f$.
	\item Étudier la continuité et la dérivabilité de $f$ en $x=-1$ et $x=1$.
	\item Étudier les variations de $f$.
	\item Démontrer que l'équation $f(x)=0$ admet deux solutions, l'une notée $x_0$ que l'on précisera et l'autre notée $\alpha$ ; $\alpha\in]-2\,;-1[$.
	\item Démontrer que la parabole $(P)$ d'équation $y=-x^2-2x-2$ est asymptote à $(\mathscr{C})$. Préciser l'autre asymptote.
	\item Tracer $(\mathscr{C})$ et $(P)$.
	\item Calculer en cm$^2$ l'aire du domaine plan limité par la courbe $(\mathscr{C})$, les droites d'équations $y=1$, $x=1$ et $x=0$.
	\item On définit l'application $g = h\circ S_{(\Delta)}$, où $h$ est l'homothétie de centre $O$ et de rapport $2$, $S_{(\Delta)}$ la symétrie orthogonale d'axe la droite $(\Delta)$ d'équation $y=x$.

	$(\Gamma)$ désigne la représentation graphique de la restriction de $f$ à l'intervalle $[0\,;1]$.
	\begin{enumerate}
		\item Caractériser $g$.
		\item Construire les points $J'$, $A'$, $I'$ et $B'$, images respectives par $g$ des points $J(0\,;1)$, $A\!\left(\dfrac12\,;2\right)$, $I(1\,;0)$ et $B(1\,;1)$.
		\item Tracer $(\Gamma')$, image de $(\Gamma)$ par $g$.
		\item En déduire l'aire du domaine plan limité par $(\Gamma')$, les droites $(J'B')$, $(OJ')$ et $(OI')$.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°11}

Ce problème comporte trois parties A, B et C. Les parties B et C sont indépendantes.

Le plan est rapporté à un repère $(O,\vec{u},\vec{v})$, l'unité graphique étant égale à $1$ cm.

\subsection*{Partie A}

On considère la fonction $f$ définie sur $[0\,;+\infty[$ par : $f(x)=x-4\sqrt{x}+4$.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Étudier la dérivabilité de $f$ en $0$. En déduire une interprétation géométrique.
		\item Étudier les variations de $f$.
		\item Tracer la courbe représentative $(\mathscr{C})$ de $f$ dans le repère $(O,\vec{u},\vec{v})$. On utilisera la feuille de papier millimétré dans le sens horizontal. On placera l'axe $Oy$ à $3$ cm du bord gauche de la feuille et l'axe $Ox$ à $3$ cm du bas de la feuille.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Soit $g$ la restriction de $f$ à $[4\,;+\infty[$. Démontrer que $g$ est une bijection de $[4\,;+\infty[$ sur $[0\,;+\infty[$ et que son application réciproque $g^{-1}$ est définie sur $[0\,;+\infty[$ par : $g^{-1}(x)=x+4\sqrt{x+4}$.
		\item Tracer la courbe représentative $(\mathscr{C}')$ de $g^{-1}$ dans le même repère que $(\mathscr{C})$. On note $(H)$ la courbe $(\mathscr{C})\cup(\mathscr{C}')$.
	\end{enumerate}
	\item Soit $(E)$ la courbe d'équation
	\[
	x^2+y^2-2xy-8x-8y+16=0
	\]
	dans le repère $(O,\vec{u},\vec{v})$.
	\begin{enumerate}
		\item Démontrer que pour tous les réels $x$ et $y$ positifs, on a :
		\[
		x^2+y^2-2xy-8x-8y+16=0 \iff [y-f(x)][y-g^{-1}(x)]=0.
		\]
		En déduire que : $(H)=(E)$.
		\item Démontrer que si un point $M(a\,;b)$ appartient à $(E)$ alors le point $M'(b\,;a)$ appartient également à $(E)$.

		En déduire que la courbe $(E)$ admet un axe de symétrie. Préciser cet axe.
	\end{enumerate}
	\item Calculer l'aire de la partie du plan limitée par la courbe $(\mathscr{C})$, l'axe des abscisses et l'axe des ordonnées.
\end{enumerate}

\subsection*{Partie B}

\begin{enumerate}
	\item Soit $m$ un nombre réel appartenant à l'intervalle $]-2\,;2[$.
	\begin{enumerate}
		\item Soit les points $A_m$ de coordonnées $(2+m\,;0)$ et $B_m$ de coordonnées $(0\,;2-m)$.

		Écrire une équation de la droite $(D_m)$ passant par $A_m$ et $B_m$.
		\item Soit $(\Delta_m)$ la droite d'équation : $x-y-2m=0$.

		Démontrer que le point d'intersection $T_m$ des droites $(D_m)$ et $(\Delta_m)$ a pour coordonnées $\left(\dfrac14(2+m)^2\,;\dfrac14(2-m)^2\right)$.
		\item Démontrer que la droite $(D_m)$ est tangente à $(\mathscr{C})$ au point $T_m$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Soit $H_m$ le projeté orthogonal de $T_m$ sur la droite $(\delta)$ d'équation $y=-x$.

		Démontrer que le point $H_m$ a pour coordonnées $(m\,;-m)$.
		\item Soit $F$ le point de coordonnées $(2\,;2)$.

		Démontrer que le quadrilatère $A_mH_mB_mF$ est un carré pour tout $m$ appartenant à $]-2\,;2[$.
	\end{enumerate}
	\item Pour $m=\dfrac12$, placer le point $T_m$, tracer les droites $(D_m)$, $(\Delta_m)$ et le carré $A_mH_mB_mF$.
\end{enumerate}

\subsection*{Partie C}

\begin{enumerate}
	\item Soit $M$ un point d'affixe $z$.
	\begin{enumerate}
		\item Démontrer que le point $H$ d'affixe $\dfrac{z-\ii\bar{z}}{2}$ est le projeté orthogonal de $M$ sur la droite $(\delta)$.
		\item Démontrer que la distance de $M$ à $(\delta)$ est égale à $\left|\dfrac{z+\ii\bar{z}}{2}\right|$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Démontrer que l'ensemble des points $M$ d'affixe $z$ telle que $\left|\dfrac{z+\ii\bar{z}}{2}\right| = |z-(2+2\ii)|$ est la courbe $(E)$.
		\item Interpréter géométriquement ce résultat. En déduire la nature de la courbe $(E)$. En donner deux éléments caractéristiques.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°12}

On considère la fonction $f$ définie sur $\R$ par : $f(x)=\dfrac{1}{x+\e^x}$.

On désigne par $(\mathscr{C})$ la courbe représentative de $f$ dans le plan muni d'un repère orthonormé $(O,I,J)$, l'unité graphique étant égale à $4$ cm.

\subsection*{Partie A}

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Calculer $f'(x)$ pour tout nombre réel $x$, puis étudier le signe de $f'(x)$ suivant les valeurs de $x$. En déduire le sens de variation de $f$.
		\item Déterminer les limites de $f$ en $+\infty$ et $-\infty$ et dresser le tableau de variation de $f$.
	\end{enumerate}
	\item Démontrer que le point $A$ de coordonnées $\left(0\,;\dfrac12\right)$ est un centre de symétrie de $(\mathscr{C})$.
	\item Déterminer une équation de la tangente $(T)$ à $(\mathscr{C})$ au point $A$.
	\item Soit $\varphi$ la fonction définie par : $\varphi(x)=\dfrac12-\dfrac14 x-f(x)$.
	\begin{enumerate}
		\item Démontrer que : $\forall x\in\,]-\infty\,;0[$, $\varphi(x)>0$ ; $\forall x\in\,]0\,;+\infty[$, $\varphi(x)<0$.
		\item En déduire la position de $(T)$ par rapport à $(\mathscr{C})$.
		\item Tracer $(T)$ et $(\mathscr{C})$.
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B}

Pour tout nombre réel non nul $m$, on considère les fonctions $f_m$ définies par : $f_m(x)=f\!\left(\dfrac{x}{m}\right)$.

$(\mathscr{C}_m)$ désigne la courbe représentative de $f_m$ dans le repère $(O,I,J)$.

Soit $T_m$ la transformation du plan dans lui-même qui à tout point $M$ associe le point $M'$ tel que : $\overrightarrow{HM'}=m\,\overrightarrow{HM}$, où $H$ est le projeté orthogonal de $M$ sur $(OJ)$.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Déterminer la nature de $T_{-1}$.
		\item Démontrer que $(\mathscr{C}_m)$ est l'image par $T_m$ de $(\mathscr{C})$.
		\item Tracer $(\mathscr{C}_{-1})$.
	\end{enumerate}
	\item Soit $\lambda$ un nombre réel. On pose : $\displaystyle I_m(\lambda)=\int_{-\lambda}^{\lambda} f_m(x)\,dx$.

	Calculer $I_m(\lambda)$ et en déduire que $I_m(\lambda)$ est indépendant de $m$.
\end{enumerate}

\subsection*{Partie C}

Soit $g$ la fonction définie sur $\R$ par : $g(x)=x-f(x)$.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Étudier le sens de variation de $g$.
		\item Calculer les limites de $g$ en $+\infty$ et $-\infty$.
		\item En déduire que l'équation $f(x)=x$ admet une solution unique $\alpha$ et que $\dfrac14\leqslant\alpha\leqslant\dfrac12$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Démontrer que : $\forall x\in\left[\dfrac14\,;\dfrac12\right]$, $f(x)\in\left[\dfrac14\,;\dfrac12\right]$.
		\item Calculer $f''(x)$.

		En déduire que : $\forall x\in\left[\dfrac14\,;\dfrac12\right]$, $|f''(x)|\leqslant\dfrac14$.
		\item En déduire que : $\forall x\in\left[\dfrac14\,;\dfrac12\right]$, $|f(x)-\alpha|\leqslant\dfrac14|x-\alpha|$.
	\end{enumerate}
	\item Soit $(U_n)$ la suite de nombres réels définie par la relation de récurrence : $U_0=\dfrac14$ et $\forall n\in\N$, $U_{n+1}=f(U_n)$.
	\begin{enumerate}
		\item Démontrer que : $\forall n\in\N$, $U_n\in\left[\dfrac14\,;\dfrac12\right]$.
		\item Démontrer que : $\forall n\in\N$, $|U_{n+1}-\alpha|\leqslant\dfrac14|U_n-\alpha|$.
		\item En déduire que : $\forall n\in\N$, $|U_n-\alpha|\leqslant\left(\dfrac14\right)^{n+1}$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Déterminer la limite de $(U_n)$.
		\item Trouver le plus petit entier naturel $p$ tel que : $|U_p-\alpha|\leqslant10^{-2}$.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°13}

Dans tout le problème, $n$ désigne un entier naturel non nul. On considère la fonction $f_n$ de $\R$ vers $\R$ définie par : $f_n(x)=x^n\e^{-x}$.

On note $\mathscr{C}_n$ la courbe représentative de $f_n$ dans le plan muni d'un repère orthonormé, l'unité graphique étant égale à $3$ cm.

\subsection*{Partie A}

\begin{enumerate}
	\item Étudier les variations de $f_1$ et déterminer sa limite en $+\infty$. Tracer $\mathscr{C}_1$ en précisant la tangente à l'origine.
	\item Pour tout entier $n$ supérieur ou égal à $2$, étudier les variations de $f_n$.

	Tracer $\mathscr{C}_3$ dans un autre repère, en précisant la tangente à l'origine.
	\item On note $S_n$ la symétrie orthogonale d'axe la droite d'équation $x=n$ et $\mathscr{C}'_n$ l'image de $\mathscr{C}_n$ par $S_n$.
	\begin{enumerate}
		\item $M$ étant le point du plan de coordonnées $(x\,;y)$, calculer les coordonnées $(x'\,;y')$ de son image $M'$ par $S_n$.
		\item Démontrer que $\mathscr{C}'_n$ est l'ensemble des points $M$ dont les coordonnées $(x\,;y)$ vérifient : $x\leqslant2n$ et $y=f_n(2n-x)$.
		\item Tracer $\mathscr{C}'_3$ dans le même repère que $\mathscr{C}_3$.

		Pour $x\leqslant2n$, on pose : $g_n(x)=f_n(2n-x)$.
		\item En interprétant géométriquement les intégrales, justifier l'égalité :
		\[
		\int_n^{2n} f_n(t)\,dt = \int_0^n g_n(t)\,dt.
		\]
	\end{enumerate}
	\item Pour tout $x$ élément de $[0\,;n]$, on pose : $h_n(x)=\ln(g_n(x))-\ln(f_n(x))$.
	\begin{enumerate}
		\item De l'étude des variations de $h_n$, déduire le signe de $h_n(x)$.
		\item Démontrer que pour tout $x$ appartenant à l'intervalle $[0\,;n]$ on a : $f_n(x)\leqslant g_n(x)$.
		\item Déduire de ce qui précède l'inégalité :
		\[
		\int_0^n f_n(t)\,dt \leqslant \int_n^{2n} f_n(t)\,dt.
		\]
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B}

Pour tout nombre réel positif $x$, on pose : $\displaystyle F_n(x)=\int_0^x f_n(t)\,dt$.

\begin{enumerate}
	\item Démontrer que la fonction $F_n$ est croissante sur $[0\,;+\infty[$.
	\item À l'aide d'intégrations par parties :
	\begin{enumerate}
		\item Calculer $F_1(x)$.
		\item Démontrer que, pour tout nombre réel positif $x$ et pour tout entier $n$ supérieur ou égal à $1$, on a :
		\[
		F_{n+1}(x) = (n+1)F_n(x) - f_{n+1}(x).
		\]
	\end{enumerate}
	\item En déduire à l'aide d'un raisonnement par récurrence, que pour tout nombre réel positif $x$ et pour tout entier $n$ supérieur ou égal à $1$ :
	\[
	F_n(x) = n!\left[1-\e^{-x}\left(1+\frac{x}{1!}+\frac{x^2}{2!}+\cdots+\frac{x^n}{n!}\right)\right].
	\]
	\item
	\begin{enumerate}
		\item Démontrer que : $\displaystyle\lim_{x\to+\infty} F_n(x) = n!$.
		\item Démontrer que pour tout nombre réel positif ou nul $x$, on a : $F_n(x)\leqslant n!$.
	\end{enumerate}
\end{enumerate}

\subsection*{Partie C}

\begin{enumerate}
	\item Démontrer que pour tout entier $n$ supérieur ou égal à $1$ on a :
	\[
	F_n(n) + \int_n^{2n} f_n(t)\,dt \leqslant n!.
	\]
	\item Déduire des résultats des parties A et B que, pour tout entier $n$ supérieur ou égal à $1$, on a :
	\[
	0 \leqslant F_n(n) \leqslant \frac{n!}{2}\,;
	\qquad
	\frac12\e^n \leqslant 1+\frac{n}{1!}+\frac{n^2}{2!}+\cdots+\frac{n^n}{n!} \leqslant \e^n.
	\]
\end{enumerate}


\section*{Sujet type bac n°14}

Le problème a pour objet l'étude d'une suite de fonctions, d'une suite d'intégrales, puis la recherche d'une valeur approchée d'une équation du type : $f(x)=k$.

\subsection*{Partie A}

On note $f_n$ la fonction numérique de la variable réelle définie sur $]-\infty\,;-2[\,\cup\,]-2\,;+\infty[$ par : $f_n(x)=\dfrac{\e^{1+x}}{(x+2)^n}$, pour $n$ entier naturel non nul.

$(\mathscr{C}_n)$ désigne la courbe représentative de $f_n$ dans le plan muni d'un repère orthonormal $(O,\vec{\imath},\vec{\jmath})$, l'unité graphique étant égale à $2$ cm.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Étudier les limites de $f_n$ en $-\infty$ et en $+\infty$. (Pour la limite en $+\infty$, on posera : $X=x+2$).
		\item Étudier suivant la parité de $n$, la limite de $f_n$ en $-2$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Calculer $f_n'(x)$, puis étudier son signe suivant la parité de $n$.
		\item Dresser le tableau de variation de $f_n$.
	\end{enumerate}
	\item Démontrer que toutes les courbes $(\mathscr{C}_n)$ passent par un point fixe $A$.

	Déterminer une équation de la tangente $(T_n)$ à $(\mathscr{C}_n)$ en $A$.
	\item
	\begin{enumerate}
		\item Calculer $\displaystyle\lim_{x\to+\infty} \frac{f_n(x)}{x}$, puis interpréter graphiquement ce résultat.
		\item Démontrer que pour tout entier $n$ non nul, et pour tout nombre réel $x$ différent de $-2$, on a :
		\[
		f_n'(x) = f_n(x) - n\cdot f_{n+1}(x).
		\]
		\item En déduire les positions relatives des courbes $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$. Représenter graphiquement $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$.
	\end{enumerate}
\end{enumerate}

\subsection*{Partie B}

Soit la suite $(u_n)$ définie par : $\displaystyle u_n = \int_{-1}^0 f_n(x)\,dx$, pour tout entier naturel $n$ non nul.

\begin{enumerate}
	\item Démontrer que la suite $(u_n)$ est décroissante et que pour tout $n$ non nul, on a : $u_n\geqslant0$. Que peut-on en déduire ?
	\item Démontrer que pour tout entier naturel $n$ supérieur ou égal à $2$, on a :
	\[
	\frac{1-2^{-n+1}}{n-1} \leqslant u_n \leqslant \frac{1-2^{-n+1}}{n-1}\,\e.
	\]
	En déduire la limite de la suite $(u_n)$.
	\item
	\begin{enumerate}
		\item En utilisant une intégration par parties, démontrer que pour tout entier naturel $n$ supérieur ou égal à $2$ on a :
		\[
		n\,u_{n+1} = 1+u_n-\frac{\e}{2^n}.
		\]
		\item Retrouver ce résultat en utilisant la relation de la question 4.b) de la partie A.
		\item En déduire que : $\displaystyle\lim_{n\to+\infty} \int_{-1}^0 \frac{n\,\e^{1+x}}{(x+2)^{n+1}}\,dx = 1$.
	\end{enumerate}
\end{enumerate}

\subsection*{Partie C}

On considère la fonction $g$ définie sur $[0\,;+\infty[$ par : $g(x)=f_1(x-1)$. On note $(\Gamma)$ sa courbe représentative dans le repère $(O,\vec{\imath},\vec{\jmath})$.

\begin{enumerate}
	\item Construire la courbe $(\Gamma)$ à partir de la courbe $(\mathscr{C}_1)$. Justifier la construction.
	\item On note $\varphi$ la fonction définie sur $[0\,;+\infty[$ par : $\varphi(x)=1+\ln(1+x)$.
	\begin{enumerate}
		\item Démontrer que l'équation $\varphi(x)=x$ admet une solution unique $\alpha$ dans l'intervalle $I=[2\,;3]$.
		\item Démontrer que pour $x$ positif, l'équation $\varphi(x)=x$ est équivalente à l'équation $g(x)=\e$.
		\item Démontrer que pour tout $x$ de $I$, $|\varphi'(x)|\leqslant\dfrac13$.
	\end{enumerate}
	\item Soit $(v_n)$ la suite définie pour tout entier naturel $n$ par : $v_0=2$ et $v_{n+1}=\varphi(v_n)$.
	\begin{enumerate}
		\item Démontrer que la suite $(v_n)$ est croissante et majorée par $\alpha$. Conclure.
		\item Démontrer que : $\varphi(I)\subset I$.
		\item Démontrer par récurrence, que pour tout entier naturel $n$, $v_n$ appartient à l'intervalle $I$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Démontrer que pour tout entier naturel $n$, on a : $|v_{n+1}-\alpha|\leqslant\dfrac13|v_n-\alpha|$, puis que : $\alpha-v_{n+1}\leqslant\dfrac13(\alpha-v_n)$.
		\item En déduire que, pour tout entier naturel $n$, $\alpha-v_n\leqslant\dfrac13$ et que la suite $(v_n)$ converge vers $\alpha$.
		\item Déterminer un entier naturel $p$ pour lequel $v_p$ est une valeur approchée de $\alpha$ à $10^{-3}$ près. Calculer cette valeur approchée.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°15}

On désigne par $R=(O,\vec{\imath},\vec{\jmath})$ un repère orthonormal du plan et $a$, $b$, $c$ trois nombres réels.

Les fonctions $f_0$, $f_1$, $f_2$ et $f$ sont respectivement définies sur l'ensemble $\R$ des nombres réels par :
\[
f_0(x)=\e^{-x/2}, \qquad f_1(x)=x\,\e^{-x/2}, \qquad f_2(x)=x^2\,\e^{-x/2}
\]
et $f(x)=(a+bx+cx^2)\,\e^{-x/2}$.

On désignera par $(\mathscr{C}_0)$, $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$ les courbes représentatives de $f_0$, $f_1$ et $f_2$ dans le repère $R$.

\subsection*{Partie A}

On se propose de représenter $(\mathscr{C}_0)$, $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$.

\begin{enumerate}
	\item Étudier les variations des fonctions $f_0$, $f_1$ et $f_2$.
	\item Préciser par leurs coordonnées dans le repère $R$, les points d'intersection de $(\mathscr{C}_0)$ et $(\mathscr{C}_1)$, $(\mathscr{C}_0)$ et $(\mathscr{C}_2)$, $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$.
	\item Étudier dans l'ensemble $\R$ des nombres réels, le signe de chacune des expressions :
	\[
	f_1(x)-f_0(x), \qquad f_2(x)-f_0(x) \qquad \text{et} \qquad f_2(x)-f_1(x).
	\]
	\item Tracer $(\mathscr{C}_0)$, $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$ dans le repère $R$ et hachurer le domaine fermé $(E)$ déterminé par les trois courbes $(\mathscr{C}_0)$, $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$, c'est-à-dire l'ensemble $(E)$ des points $M$ du plan, dont les coordonnées $(x\,;y)$ dans le repère $R$ vérifient :
	\[
	\begin{cases} -1\leqslant x\leqslant0 \\ f_2(x)\leqslant y\leqslant f_0(x) \end{cases}
	\quad\text{ou}\quad
	\begin{cases} 0\leqslant x\leqslant1 \\ f_1(x)\leqslant y\leqslant f_0(x) \end{cases}.
	\]
\end{enumerate}

\subsection*{Partie B}

On se propose de calculer l'aire de $(E)$.

\begin{enumerate}
	\item Soit $n$, un entier naturel quelconque. Démontrer qu'en posant $\displaystyle I_n(x)=\int_0^x t^n\,\e^{-t/2}\,dt$, on définit une fonction $I_n$ qui a l'ensemble $\R$ des nombres réels pour ensemble de définition.
	\item Soit $n$ un entier naturel et $x$ un nombre réel quelconque. Démontrer que :
	\[
	I_{n+1}(x) = -2x^{n+1}\e^{-x/2} + 2(n+1)I_n(x).
	\]
	\item Soit $x$ un nombre réel quelconque.
	\begin{enumerate}
		\item Calculer $I_0(x)$ en fonction de $x$.
		\item En déduire les expressions de $I_1(x)$ et $I_2(x)$ en fonction de $x$.
	\end{enumerate}
	\item Calculer l'aire de $(E)$.
\end{enumerate}

\subsection*{Partie C}

On se propose de calculer la dérivée d'ordre $n$ de $f$.

On désigne par $f^{(0)}$ (ou $f$), $f^{(1)}$ (ou $f'$), $f^{(2)}$ (ou $f''$), $f^{(3)}$ (ou $f'''$), etc. les dérivées successives de la fonction $f$.

\begin{enumerate}
	\item Démontrer par récurrence, que pour tout entier naturel $n$, on peut trouver trois nombres réels $\alpha_n$, $\beta_n$ et $\gamma_n$ vérifiant pour tout nombre réel $x$ :
	\[
	f^{(n)}(x) = (\alpha_n+\beta_nx+\gamma_nx^2)\,\e^{-x/2}.
	\]
	On trouvera $\alpha_0=a$, $\beta_0=b$ et $\gamma_0=c$ et pour chaque entier naturel $n$, le raisonnement par récurrence montrera que $\alpha_{n+1}$, $\beta_{n+1}$ et $\gamma_{n+1}$ vérifient :
	\[
	\alpha_{n+1}=\beta_n-\frac12\alpha_n, \qquad \beta_{n+1}=2\gamma_n-\frac12\beta_n \qquad \text{et} \qquad \gamma_{n+1}=-\frac12\gamma_n.
	\]
	\item Pour chaque entier naturel $n$, on pose : $\beta'_n=\beta_n+4n\gamma_n$ et $\alpha'_n=\alpha_n+2n\beta_n+4n(n+1)\gamma_n$.

	Démontrer que $(\gamma_n)$, $(\beta'_n)$ et $(\alpha'_n)$ sont des suites géométriques de raison $-\dfrac12$.
	\item En déduire pour chaque entier naturel $n$, l'expression de $\alpha_n$, $\beta_n$ et $\gamma_n$ en fonction de $n$, $a$, $b$ et $c$.
\end{enumerate}


\section*{Sujet type bac n°16}

Le problème est composé de l'étude d'une suite de fonctions dépendant d'un paramètre, puis de la recherche d'une valeur approchée d'une solution d'une équation du type : $f(x)=x$.

\subsection*{Partie A}

Pour tout entier $n$ strictement positif, on note $f_n$ la fonction numérique de la variable réelle définie sur $\R\setminus\{-1\}$ par : $f_n(x)=\dfrac{\e^x}{(x+1)^n}$.

On note $(\mathscr{C}_n)$ la courbe représentative de $f_n$ dans le plan rapporté à un repère orthonormé (unité graphique : $2$ cm).

\begin{enumerate}
	\item Déterminer la fonction dérivée $f_n'$ de $f_n$ et donner l'expression de $f_n'$ en fonction de $f_n$ et de $f_{n+1}$.
	\item Étudier les variations de $f_n$ et ses limites éventuelles en $-\infty$, $-1$ et $+\infty$. (On distinguera les cas où $n$ est pair et $n$ est impair.)
	\item Démontrer que toutes les courbes $(\mathscr{C}_n)$ passent par un même point.
	\item Déterminer la limite de $\dfrac{f_n(x)}{x}$ lorsque $x$ tend vers $+\infty$.

	Que peut-on en déduire pour les courbes $(\mathscr{C}_n)$ ?

	Tracer sur deux figures distinctes les courbes $(\mathscr{C}_1)$ et $(\mathscr{C}_2)$.
\end{enumerate}

\subsection*{Partie B}

Pour tout entier $n$ strictement positif, on note : $\displaystyle I_n=\int_0^1 f_n(x)\,dx$.

\begin{enumerate}
	\item Démontrer que la suite $(I_n)_{n\in\N^*}$ est décroissante et qu'elle converge.
	\item Démontrer que pour tout entier naturel $n$ supérieur ou égal à $2$, on a :
	\[
	\frac{1}{n-1}\left(1-\frac{1}{2^{n-1}}\right) \leqslant I_n \leqslant \frac{\e}{n-1}\left(1-\frac{1}{2^{n-1}}\right).
	\]
	En déduire la limite de la suite $(I_n)_{n\in\N^*}$.
	\item Démontrer en utilisant la relation de la question A.1., une relation entre $I_n$ et $I_{n+1}$.
	\item Démontrer que : $\displaystyle\lim_{n\to+\infty} n\,I_{n+1} = 1$.

	En déduire que la suite $(n\,I_n)_{n\in\N^*}$ converge et déterminer sa limite.
\end{enumerate}

\subsection*{Partie C}

Dans cette partie, $n=2$.

\begin{enumerate}
	\item Démontrer que l'équation $f_2(x)=x$ admet une solution unique $\alpha$ dans l'intervalle $\left[\dfrac12\,;1\right]$.

	Le but de cette partie est de déterminer une valeur approchée de $\alpha$.
	\item Étudier les variations de $f_2'$ dans $\left[\dfrac12\,;1\right]$ et en déduire que pour tout nombre réel $x$ appartenant à l'intervalle $\left[\dfrac12\,;1\right]$, on a : $-0,25\leqslant f_2'(x)\leqslant0$.
	\item Soit $(u_n)_{n\in\N}$ la suite définie par : $u_0=1$ et pour tout $n$ de $\N$, $u_{n+1}=f_2(u_n)$.
	\begin{enumerate}
		\item Démontrer que pour tout entier naturel $n$, $u_n$ est élément de $\left[\dfrac12\,;1\right]$.
		\item Démontrer, en utilisant la question C.2., que pour tout entier naturel $n$ : $|u_{n+1}-\alpha|\leqslant\dfrac14|u_n-\alpha|$.
		\item En déduire que pour tout entier naturel $n$, $|u_n-\alpha|\leqslant\dfrac12\left(\dfrac14\right)^n$ et que la suite $(u_n)_{n\in\N}$ converge vers $\alpha$.
		\item Pour quelles valeurs de $n$, $u_n$ est-elle une valeur approchée de $\alpha$ à $10^{-3}$ près ?
	\end{enumerate}
\end{enumerate}

(On donne : $\sqrt{\e}\approx1,65$ ; $\ln2\approx0,69$ ; $\ln5\approx1,6$.)


\section*{Sujet type bac n°17}

\subsection*{Partie A}

Soit $\Phi$ la fonction numérique définie par : $\Phi(x)=\dfrac{x}{(\ln x)^2}$, si $x>0$, $\Phi(0)=0$.

\begin{enumerate}
	\item Démontrer que $\Phi$ est une fonction continue sur son ensemble de définition.
	\item
	\begin{enumerate}
		\item Étudier la dérivabilité de $\Phi$ en $0$.
		\item Dresser le tableau de variation de la fonction $\Phi$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Étudier le comportement asymptotique de $\Phi$ pour les grandes valeurs de $x$.
		\item Préciser la tangente $(T)$ à la courbe $(\mathscr{C})$ représentative de $\Phi$ au point d'abscisse $\e$.
		\item Tracer $(\mathscr{C})$ et $(T)$ dans un repère orthonormé d'unité $1$ cm.
	\end{enumerate}
	\item Démontrer que l'équation $\Phi(x)=\e$ admet exactement deux solutions dans l'intervalle $]1\,;+\infty[$ et que l'une de ces solutions est comprise entre $\e^3$ et $\e^4$.
\end{enumerate}

\subsection*{Partie B}

Dans cette partie, on considère la fonction $f$ définie par
\[
f : \ ]1\,;+\infty[\ \to \R, \qquad x \mapsto \int_{\e}^{x} \frac{dt}{\ln t}.
\]
Le but de cette partie est de représenter graphiquement cette fonction sans connaître l'expression explicite de $f'(x)$.

\begin{enumerate}
	\item
	\begin{enumerate}
		\item Justifier l'existence de $f$.
		\item Démontrer que $f$ est croissante sur $]1\,;+\infty[$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Établir que pour tout nombre réel $t$ appartenant à $]1\,;+\infty[$, on a : $\ln t < t-1$.
		\item En déduire une minoration de $f(x)$ lorsque $x>\e$. Préciser alors la limite de $f(x)$ lorsque $x$ tend vers $+\infty$.
		\item De façon analogue, calculer la limite de $f(x)$ lorsque $x$ tend vers $1$ par valeurs supérieures.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Soient $a$ et $b$ deux nombres réels tels que : $\e\leqslant a<b$.

		Établir que : $\dfrac{b-a}{\ln b} \leqslant \displaystyle\int_a^b \frac{dt}{\ln t} \leqslant \dfrac{b-a}{\ln a}$.
		\item Soit $x$ un nombre réel strictement supérieur à $\e$.

		Démontrer que : $\forall u\in[\e\,;x]$, $\dfrac{x-u}{\ln x} \leqslant f(x) \leqslant u + \dfrac{x-u}{\ln u}$.
		\item Résoudre l'inéquation $\Phi(x)<x$. Justifier que l'on peut choisir $u=\Phi(x)$, lorsque $x>\e^4$. Établir alors que :
		\[
		\forall x>\e^4, \quad 1-\frac{\Phi(x)}{x} \leqslant \left(\frac{\ln x}{x}\right)f(x) \leqslant \frac{1}{\ln x} + \left[1-\frac{\Phi(x)}{x}\right]\frac{\ln x}{\ln\Phi(x)}.
		\]
		\item Démontrer que : $\displaystyle\lim_{x\to+\infty} \frac{\ln x}{\ln\Phi(x)}= 1$ ;

		en déduire que : $\displaystyle\lim_{x\to+\infty} \left(\frac{\ln x}{x}\right)f(x) = 1$.
		\item Vérifier que : $\forall x>\e^4$, $f(x)=\dfrac{x}{\ln x}\bigl[1+\varepsilon(x)\bigr]$, avec $\displaystyle\lim_{x\to+\infty} \varepsilon(x)=0$.
		\item En déduire le comportement asymptotique de la fonction $f$ lorsque $x$ tend vers $+\infty$.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item On cherche à obtenir une valeur approchée de $f(2)$. Soit $h : t\mapsto at+b$ la fonction telle que :
		\[
		h(\e) = 1 \qquad \text{et} \qquad h(2) = \frac{1}{\ln2}.
		\]
		Calculer les nombres réels $a$ et $b$ et donner deux valeurs approchées de ces nombres à $10^{-2}$ près.

		On utilisera ces valeurs approchées pour la question 4.b).
		\item On prend alors : $\displaystyle f(2) \approx \int_{\e}^{2} h(t)\,dt$. Calculer cette valeur.
	\end{enumerate}
	\item
	\begin{enumerate}
		\item Donner le tableau de variation de $f$.
		\item Construire la courbe représentative de la fonction $f$.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°18}

On considère l'application $f$ de $]-1\,;+\infty[$ dans $\R$ définie par : $f(x)=\dfrac{\ln(1+x)}{x}$, si $x\neq0$, $f(0)=1$.

On note $(\mathscr{C})$ la courbe représentative de $f$ dans le plan rapporté à un repère orthonormé $(O,\vec{\imath},\vec{\jmath})$.

\subsection*{Partie A}

\begin{enumerate}
	\item Étudier la continuité de $f$ sur $]-1\,;+\infty[$.
	\item Étudier la dérivabilité de $f$ sur $]-1\,;+\infty[$. Expliciter la fonction dérivée $f'$.
	\item On note $g$ l'application de $]-1\,;+\infty[$ dans $\R$ définie par : $g(x)=\dfrac{x}{1+x}-\ln(1+x)$.
	\begin{enumerate}
		\item Étudier les variations de $g$ et le signe de $g(x)$. (On ne demande pas l'étude de la limite de $g$ en $-1$.)
		\item En déduire les variations de $f$.
	\end{enumerate}
	\item Étudier les limites de $f$ aux bornes de l'intervalle $]-1\,;+\infty[$.
	\item Construire la courbe $(\mathscr{C})$. Préciser les asymptotes et la position de $(\mathscr{C})$ par rapport à l'axe des abscisses.
	\item Déterminer une équation de la tangente à $(\mathscr{C})$ au point d'abscisse $0$ et étudier la position de $(\mathscr{C})$ par rapport à cette tangente. (On étudiera les variations de l'application $h$ de $]-1\,;+\infty[$ dans $\R$ définie par : $h(x)=x^2\left(\dfrac12+f'(x)\right)$, puis le signe de $h(x)$).
\end{enumerate}

\subsection*{Partie B}

\begin{enumerate}
	\item Démontrer qu'il existe un unique nombre réel $\alpha$ de l'intervalle $]0\,;1[$ tel que : $f(\alpha)=\alpha$. (On ne demande pas de calculer $\alpha$).
	\item On considère la suite $(u_n)$ définie par : $u_0=\dfrac12$ et pour tout $n\in\N$, $u_{n+1}=f(u_n)$.
	\begin{enumerate}
		\item Démontrer que : $\forall n\in\N$, $0\leqslant u_n\leqslant1$.
		\item Démontrer que : $\forall n\in\N$, $|u_{n+1}-\alpha|\leqslant\dfrac12|u_n-\alpha|$.

		(On remarquera que $u_{n+1}-\alpha=f(u_n)-f(\alpha)$ et on utilisera le résultat : $-\dfrac12\leqslant f'(x)\leqslant0$.)
		\item En déduire que la suite $(u_n)$ converge vers $\alpha$.
	\end{enumerate}
\end{enumerate}


\section*{Sujet type bac n°19}

Le plan est muni d'un repère orthonormé $(O,I,J)$, l'unité graphique étant égale à $1$ cm.

\subsection*{Partie A}

On considère dans $\C$ l'équation :
\[
z^3-(6+\ii\sqrt3)z^2+(11+4\ii\sqrt3)z-6-3\ii\sqrt3=0.
\]

\begin{enumerate}
	\item Résoudre cette équation en sachant qu'elle a deux solutions réelles.
	\item On désigne par $A$, $B$, $C$, $E$ et $G$ les points d'affixes respectives $3$, $2+\ii\sqrt3$, $-1$, $7$ et $11+4\ii\sqrt3$.
	\begin{enumerate}
		\item Démontrer que le triangle $IAB$ est équilatéral.
		\item Démontrer que les points $B$, $C$ et $G$ sont alignés.
		\item Placer les points $A$, $B$, $C$, $E$ et $G$.
	\end{enumerate}
	\item Calculer l'affixe du point $F$ de l'axe des abscisses tel que le triangle $EFG$ soit équilatéral.
\end{enumerate}

\subsection*{Partie B}

On désigne par $O'$ le centre de gravité du triangle $IAB$.

\begin{enumerate}
	\item On veut déterminer une homothétie $h$ qui transforme le triangle $IAB$ en $EFG$.
	\begin{enumerate}
		\item Démontrer que l'image par $h$ de $[IA]$ est $[EF]$.
		\item Justifier que la seule homothétie $h$ qui transforme $IAB$ en $EFG$ est telle que :
		\[
		h(I)=E, \qquad h(A)=F \qquad \text{et} \qquad h(B)=G.
		\]
		\item Caractériser $h$.
	\end{enumerate}
	\item Soit $r$ la rotation de centre $O'$ et d'angle $\dfrac{2\pi}{3}$ et $f$ la similitude directe telle que : $f=h\circ r$.
	\begin{enumerate}
		\item Donner le rapport et l'angle de $f$.
		\item Démontrer que $f$ transforme le triangle $IAB$ en $EFG$.
	\end{enumerate}
	\item Soit $g$ une similitude directe qui transforme le triangle $IAB$ en $EFG$.
	\begin{enumerate}
		\item Démontrer que $h^{-1}\circ g$ est une rotation qui laisse le triangle $IAB$ globalement invariant (c'est-à-dire que le triangle $IAB$ a pour image lui-même).
		\item Caractériser les trois rotations qui laissent globalement invariant le triangle $IAB$.
		\item En déduire que les similitudes directes qui transforment $IAB$ en $EFG$ sont $h$, $f$ et une troisième $f'$ que l'on déterminera à l'aide de $h$ et $r$.
		\item Déterminer le rapport et l'angle de $f'$.
	\end{enumerate}
	\item Soit $\Omega$ le centre de la similitude $f$. On désigne par $K$ le milieu du segment $[IA]$.
	\begin{enumerate}
		\item Déterminer l'image $K'$ de $K$ par $f$.
		\item Démontrer que $\Omega$, $A$, $G$ et $F$ sont cocycliques.
		\item Démontrer que $\Omega$, $F$, $K$ et $K'$ sont cocycliques.
		\item Construire $\Omega$.
	\end{enumerate}
	\item Déterminer l'application complexe associée à $f'$.
	\item Calculer l'affixe du centre $\Omega'$ de $f'$.
\end{enumerate}


\section*{Sujets type : Calcul matriciel}

\subsection*{Sujet 1 (Session 2026)}

Considérons les matrices carrées suivantes :
\[
A = \begin{pmatrix}1&0&2\\0&1&3\\0&0&1\end{pmatrix}, \qquad
B = \begin{pmatrix}2&3&5\\1&0&2\\0&0&-1\end{pmatrix}, \qquad
I_3 = \begin{pmatrix}1&0&0\\0&1&0\\0&0&1\end{pmatrix}.
\]
\begin{enumerate}
	\item
	\begin{enumerate}
		\item Calculer le déterminant de la matrice $A$. Conclure.
		\item Vérifier que $2A - A^2 = I_3$. En déduire la matrice $P$ telle que $A \times P = I_3$.
	\end{enumerate}
	\item On pose $D = ABP$.
	Pour tout $n \in \N^*$, montrer par récurrence que $D^n = AB^nP$.
\end{enumerate}

\subsection*{Sujet 2}

On considère les matrices carrées d'ordre $2$ :
\[
A = \begin{pmatrix}2&1\\0&3\end{pmatrix}, \qquad
B = \begin{pmatrix}1&-1\\2&0\end{pmatrix}.
\]
\begin{enumerate}
	\item Calculer $A+B$ et $2A-3B$. \hfill (0,75 pt)
	\item Calculer $AB$ et $BA$. Que remarque-t-on ? \hfill (1,0 pt)
	\item Calculer $\mathrm{tr}(A)$, $\mathrm{tr}(B)$, $\mathrm{tr}(AB)$ et $\mathrm{tr}(BA)$.
	Vérifier que $\mathrm{tr}(AB) = \mathrm{tr}(BA)$ bien que $AB \neq BA$. \hfill (1,0 pt)
	\item Déterminer la matrice transposée ${}^tA$ de $A$, et vérifier que $\mathrm{tr}({}^tA) = \mathrm{tr}(A)$. \hfill (0,5 pt)
\end{enumerate}

\subsection*{Sujet 3}

On considère la matrice $A = \begin{pmatrix}3&2\\4&3\end{pmatrix}$ et le système $(S)$ :
\[
(S) : \begin{cases} 3x + 2y = 7 \\ 4x + 3y = 10 \end{cases}
\]
\begin{enumerate}
	\item Calculer $\det(A)$. La matrice $A$ est-elle inversible ? \hfill (0,5 pt)
	\item Déterminer $A^{-1}$. \hfill (1,0 pt)
	\item Écrire le système $(S)$ sous la forme matricielle $AX = B$, où l'on précisera $X$ et $B$. \hfill (0,5 pt)
	\item En utilisant $A^{-1}$, résoudre le système $(S)$. \hfill (1,0 pt)
\end{enumerate}

\subsection*{Sujet 4}

On considère la matrice $A = \begin{pmatrix}3&-1\\1&1\end{pmatrix}$.
\begin{enumerate}
	\item Calculer $A^2$, et vérifier que $A^2 = 4A - 4I_2$. \hfill (1,0 pt)
	\item On pose $N = A - 2I_2$. Calculer $N^2$. \hfill (0,75 pt)
	\item Démontrer par récurrence que, pour tout entier $n \geqslant 1$ :
	\[
	A^n = 2^n I_2 + n\cdot 2^{n-1} N.
	\]
	\hfill (1,5 pt)
	\item Application : calculer $A^3$ à l'aide de la formule précédente, puis vérifier le résultat en calculant $A^3 = A^2 \times A$ directement. \hfill (0,75 pt)
\end{enumerate}

\subsection*{Sujet 5}

On considère la matrice carrée d'ordre $3$ :
\[
C = \begin{pmatrix}0&1&1\\1&0&1\\1&1&0\end{pmatrix}.
\]
\begin{enumerate}
	\item Calculer $C^2$. \hfill (1,0 pt)
	\item Vérifier que $C^2 - C = 2I_3$. \hfill (0,5 pt)
	\item En déduire que $C$ est inversible, et déterminer $C^{-1}$. \hfill (1,0 pt)
	\item En déduire les solutions du système $(S)$ :
	\[
	(S) : \begin{cases} y + z = 2 \\ x \phantom{{}+y{}} + z = 0 \\ x + y \phantom{{}+z{}} = 4 \end{cases}
	\]
	\hfill (1,0 pt)
\end{enumerate}

\subsection*{Sujet 6}

On donne deux matrices carrées d'ordre $3$, $M$ et $N$, telles que $\det(M) = 3$ et $\det(N) = -2$,
ainsi que deux matrices carrées d'ordre $2$, $A$ et $B$, telles que $\mathrm{tr}(A) = 5$ et $\mathrm{tr}(B) = -1$.
\begin{enumerate}
	\item Calculer $\det(MN)$, $\det(M^2N^3)$ et $\det({}^tM)$. \hfill (1,0 pt)
	\item La matrice $M$ est-elle inversible ? Justifier. \hfill (0,5 pt)
	\item Peut-on avoir $MN = NM = I_3$ (c'est-à-dire $N = M^{-1}$) ? Justifier à l'aide des valeurs de $\det(M)$ et $\det(N)$. \hfill (1,0 pt)
	\item Calculer $\mathrm{tr}(A+B)$ et $\mathrm{tr}(2A - 3B)$. \hfill (0,75 pt)
	\item On donne $AB = \begin{pmatrix}2&1\\0&3\end{pmatrix}$. Donner la valeur de $\mathrm{tr}(BA)$ sans calculer le produit $BA$. \hfill (0,5 pt)
\end{enumerate}